# PostgreSQL 連線設定
POSTGRES_HOST=localhost
POSTGRES_DB=your_database
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password
POSTGRES_PORT=5432

# 🚀 宣告式監聽配置 (優雅模式)
# 列出您想要自動掛載觸發器的資料表名稱，以逗號分隔。
# 伺服器啟動時會自動檢查並建立必要的 PostgreSQL 函式與觸發器。
WATCH_TABLES=users,posts,comments
